clusterPatches:
- target:
    kind: Cluster
    name: cluster-001
    labelSelector:
        cluster-name: "foo"
  kustomizePatch: ./kustomize/
- target:
     kind: Cluster
     name: cluster-002
     labelSelector:
        cluster-name: "bar"
  kustomizePatch: ./cluster-002/